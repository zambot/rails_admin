:ruby
  value = case field.form_value
          when Numeric
            number_with_delimiter(field.form_value)
          else
            field.form_value
          end
= form.send field.view_helper, field.method_name, field.html_attributes.reverse_merge({ value: value, class: 'form-control', required: field.required})
